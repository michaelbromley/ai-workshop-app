# Presenter password for creating questions and moderating
PRESENTER_PASSWORD=workshop2024

# Server port (default: 3000)
PORT=3000

# Database path (default: ./data/workshop.db)
DB_PATH=/app/data/workshop.db

# Node environment (development or production)
NODE_ENV=production
